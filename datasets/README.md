# Datasets

Keep this repository lightweight.

- `synthetic/` contains small generated arrays committed to git.
- `public/` should contain *pointers* (links + instructions), not large downloads.
- `format_descriptions/` describes expected file layouts and metadata.

